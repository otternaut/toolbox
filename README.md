<div align="center">

# 🦦 OtterBot

**A collection of portable, tool-agnostic AI agent skills — namespaced under OtterBot.**

Reusable instruction sets that teach coding agents — Claude Code, Codex, Cursor,
and others — how to perform specific tasks well. Every skill is namespaced with
an `otterbot-` prefix so it can't collide with other skills in a tool's shared
global directory.

[Installation](#-installation) · [Available skills](#-available-skills) · [Using a skill](#-using-a-skill) · [Contributing](#-contributing)

![License: MIT](https://img.shields.io/badge/license-MIT-blue.svg)
![Made with Bash](https://img.shields.io/badge/made%20with-bash%20%2B%20markdown-1f425f.svg)
![No dependencies](https://img.shields.io/badge/dependencies-none-brightgreen.svg)

</div>

---

Each skill is a self-contained directory of Markdown. A single install script
publishes every skill into the global skills directory of each supported tool —
so a skill authored once is available **everywhere**. Claude Code is symlinked
(edits apply live); Codex and Cursor get real copies, because they don't follow
symlinks when discovering skills. ✨

## 📚 Available skills

<!-- BEGIN SKILLS TABLE (auto-generated by scripts/update-readme — do not edit by hand) -->
| Skill | What it does |
| --- | --- |
| [`otterbot-review`](skills/otterbot-review/SKILL.md) | Perform a principal-level code review, producing a structured Markdown report with severity-tagged findings and a scorecard. |
<!-- END SKILLS TABLE -->

> 🤖 This table is generated from each skill's `SKILL.md` and refreshed
> automatically on commit — see [Keeping docs in sync](#-keeping-docs-in-sync).

## ✅ Requirements

- 🐚 **bash** and **coreutils** — preinstalled on macOS and Linux.
- 🛠️ At least one supported AI tool that reads skills from its config directory:

  | Tool | Skills directory |
  | --- | --- |
  | Claude Code | `~/.claude/skills` |
  | Codex | `~/.codex/skills` |
  | Cursor | `~/.cursor/skills` |

No language runtime, package manager, or build step is needed. 🎉

## 🚀 Installation

Clone the repository and run the installer:

```bash
git clone https://github.com/otternaut/toolbox.git
cd toolbox
./scripts/install
```

Want to see what would happen first? Preview it with a dry run:

```bash
./scripts/install --dry-run
```

How skills are published differs by tool, because only Claude Code follows
symlinks when it discovers skills:

| Tool | Published as | Edits apply |
| --- | --- | --- |
| Claude Code | 🔗 symlink | immediately |
| Codex | 📁 real copy | after re-running the installer |
| Cursor (app) | 📁 real copy | after re-running the installer |
| Cursor CLI (`cursor-agent`) | 📄 generated rule | after re-running the installer |

So edits to a skill are live instantly in Claude Code, but for the others you
re-run `./scripts/install` (or `./scripts/install --sync`) to refresh them.
Every tool also loads skills only at **startup**, so restart Codex, Cursor, or
any super.engineering session using them to pick up changes.

> 🧩 **Why the fourth row?** The Cursor Agent **CLI** (`cursor-agent`) — what
> runs when you pick the "cursor" model in super.engineering / Superconductor —
> has no skills feature; it reads Cursor **rules**. So the installer also runs
> [`scripts/export-cursor-rules`](scripts/export-cursor-rules), mirroring each
> skill into a global `~/.cursor/rules/<name>.mdc` that loads on demand by its
> `description` — the same way a skill would.

The installer is **idempotent and non-destructive**:

- ♻️ Re-running refreshes stale symlinks and drifted copies, leaving correct
  entries untouched.
- 🛡️ A real file or directory the installer didn't create is never overwritten —
  it's skipped with a warning. Copies it manages carry a `.toolbox-managed`
  marker so only those are ever refreshed.
- 📁 Missing target directories are created automatically.
- 🪝 It points this repo's git at `.githooks` (via `core.hooksPath`) so the
  README skills table stays in sync automatically — see
  [Keeping docs in sync](#-keeping-docs-in-sync).

Run `./scripts/install --help` for all options.

## 💡 Using a skill

The most portable way to invoke a skill is to **describe the task** — every tool
matches your request against each skill's `description` and loads the right one:

```
review my changes before I open a PR
```

Claude Code additionally exposes skills as slash commands, so you can name one
directly:

```
/otterbot-review https://github.com/acme/widgets/pull/42
```

Codex and Cursor don't surface skills as `/name` commands — describe the task
instead and the agent picks the skill by its `description`. 🎯

## 🔄 Keeping docs in sync

The [Available skills](#-available-skills) table is **generated**, not
hand-maintained. [`scripts/update-readme`](scripts/update-readme) reads the
`description` from each `skills/<name>/SKILL.md` and rewrites the table between
the `<!-- BEGIN/END SKILLS TABLE -->` markers.

After you run `./scripts/install`, a **pre-commit git hook**
([`.githooks/pre-commit`](.githooks/pre-commit)) runs this generator on every
commit and re-stages `README.md` if the table changed — so adding, removing, or
re-describing a skill updates the README automatically. You never edit the table
by hand. ✍️

You can also run it manually:

```bash
./scripts/update-readme          # rewrite the table now
./scripts/update-readme --check  # exit non-zero if the table is stale (for CI)
```

## 🗂️ Repository layout

```
toolbox/
├── scripts/
│   ├── install             # 🔗 Publishes skills into your AI tool folders
│   ├── export-cursor-rules # 📄 Mirrors skills into Cursor rules for cursor-agent
│   └── update-readme       # 🔄 Regenerates the skills table from frontmatter
├── .githooks/
│   └── pre-commit       # 🪝 Keeps the README table in sync on commit
└── skills/
    └── otterbot-review/
        ├── SKILL.md      # 📝 Frontmatter + instructions
        ├── references/   # 📎 Long-form docs loaded on demand
        └── evals/        # 🧪 Eval prompts for the skill
```

## 🤝 Contributing

New skills and improvements are welcome! See
[CONTRIBUTING.md](./CONTRIBUTING.md) for the skill format, conventions, and PR
workflow, and [AGENTS.md](./AGENTS.md) for the full design conventions.

## 📄 License

Released under the [MIT License](./LICENSE).
